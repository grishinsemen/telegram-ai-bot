# Инструкция по настройке MiniMax MCP в Cursor

## ✅ Что уже сделано

1. ✅ Установлен `uv` и `uvx`
2. ✅ Проверен Python
3. ✅ Создан конфигурационный файл MCP

## 📝 Следующие шаги

### Шаг 1: Получите API ключ MiniMax

1. Перейдите на [platform.minimax.io](https://platform.minimax.io/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **API Keys** (или **Настройки** → **API ключи**)
4. Создайте новый API ключ
5. Скопируйте ключ (он понадобится в следующем шаге)

### Шаг 2: Настройте API ключ в конфигурации

1. Откройте файл конфигурации:
   ```
   C:\Users\SEMEN\AppData\Roaming\Cursor\User\globalStorage\mcp.json
   ```

2. Найдите строку:
   ```json
   "MINIMAX_API_KEY": "YOUR_API_KEY_HERE"
   ```

3. Замените `YOUR_API_KEY_HERE` на ваш реальный API ключ:
   ```json
   "MINIMAX_API_KEY": "ваш_реальный_ключ_здесь"
   ```

4. Сохраните файл

### Шаг 3: Перезапустите Cursor

1. **Полностью закройте** Cursor IDE (не просто окно, а весь процесс)
2. Запустите Cursor снова
3. MCP сервер MiniMax должен автоматически подключиться

### Шаг 4: Проверьте подключение

1. В Cursor откройте **Settings** (настройки) - `Ctrl+,`
2. Найдите раздел **MCP** или **Model Context Protocol**
3. Убедитесь, что сервер `minimax` отображается как подключенный
4. Проверьте, что инструмент `text_to_audio` доступен

## 🔧 Альтернативный способ: через Command Palette

Если в настройках нет раздела MCP:

1. Нажмите `Ctrl+Shift+P` (Command Palette)
2. Введите "MCP" или "Model Context Protocol"
3. Выберите команду для управления MCP серверами
4. Проверьте статус подключения

## 📍 Расположение файла конфигурации

Файл конфигурации находится здесь:
```
C:\Users\SEMEN\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

Чтобы быстро открыть его:
- Нажмите `Win+R`
- Введите: `%APPDATA%\Cursor\User\globalStorage\mcp.json`
- Нажмите Enter

## ✅ Проверка работоспособности

После настройки вы сможете использовать Text2Speech через MCP:

- Инструмент: `text_to_audio`
- Параметры:
  - `text` - текст для преобразования
  - `voice_id` (опционально) - ID голоса
  - `speed` (опционально) - скорость речи
  - `volume` (опционально) - громкость

## 🐛 Устранение проблем

### Проблема: MCP сервер не подключается

**Решение:**
1. Проверьте правильность API ключа в файле конфигурации
2. Убедитесь, что `uvx` доступен в PATH (или используйте полный путь)
3. Проверьте логи Cursor на наличие ошибок
4. Попробуйте перезапустить Cursor еще раз

### Проблема: uvx не найден

**Решение:**
Добавьте `C:\Users\SEMEN\.local\bin` в системный PATH:
1. Откройте "Переменные среды" (Environment Variables)
2. Добавьте путь в переменную PATH
3. Перезапустите Cursor

Или используйте полный путь в конфигурации:
```json
"command": "C:\\Users\\SEMEN\\.local\\bin\\uvx.exe"
```

### Проблема: Ошибка при запуске minimax-mcp

**Решение:**
1. Проверьте, что Python 3.x установлен
2. Убедитесь, что API ключ правильный
3. Проверьте интернет-соединение (нужно для загрузки пакета)

## 📚 Дополнительные ресурсы

- [Официальная документация MiniMax MCP](https://platform.minimax.io/docs/guides/mcp-guide)
- [MiniMax API Документация](https://platform.minimax.io/docs)

