# 🚂 Развертывание на Railway.com

## Быстрая инструкция

### 1. Подготовка репозитория
Репозиторий уже готов! Все необходимые файлы созданы:
- ✅ `requirements.txt` - зависимости Python
- ✅ `Procfile` - команда запуска
- ✅ `railway.json` - конфигурация Railway
- ✅ Поддержка переменных окружения в коде

### 2. Создание проекта на Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите репозиторий `grishinsemen/telegram-ai-bot`

### 3. Настройка переменных окружения

В настройках проекта перейдите в **"Variables"** и добавьте:

#### Обязательные:
```
BOT_TOKEN=ваш_токен_от_BotFather
CHAT_ID=ваш_chat_id
ZENMUX_API_KEY=ваш_zenmux_ключ
```

#### Рекомендуемые:
```
ZENMUX_MODEL=google/gemini-3-pro-preview-free
ZENMUX_BASE_URL=https://zenmux.ai/api/v1
FALLBACK_MODELS=["google/gemini-3-pro-preview"]
OPENROUTER_API_KEY=ваш_openrouter_ключ
OPENROUTER_MODEL=openai/gpt-4o-mini
PERSONALITY=putin
```

#### Опциональные (для транскрипции):
```
ASSEMBLYAI_API_KEY=ваш_assemblyai_ключ
DEEPGRAM_API_KEY=ваш_deepgram_ключ
LEMONFOX_API_KEY=ваш_lemonfox_ключ
```

#### Опциональные (запасные LLM):
```
GROQ_API_KEY=ваш_groq_ключ
GROQ_MODEL=llama-3.3-70b-versatile
```

### 4. Настройка FFmpeg (для голосовых сообщений)

Railway использует Nixpacks для сборки. FFmpeg должен быть доступен автоматически, но если нет:

1. В настройках проекта создайте файл `nixpacks.toml`:
```toml
[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
```

Или добавьте в `railway.json`:
```json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt"
  }
}
```

### 5. Запуск

Railway автоматически:
- ✅ Определит Python проект
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит команду из `Procfile`: `python telegram_bot.py`
- ✅ Будет перезапускать бота при ошибках

### 6. Проверка работы

1. Откройте вкладку **"Logs"** в Railway
2. Вы должны увидеть:
   ```
   🤖 TELEGRAM БОТ ЗАПУЩЕН
   ✅ ZenMux.ai: подключен
   ✅ Бот найден: @your_bot_name
   📋 БОТ АКТИВЕН И СЛУШАЕТ СООБЩЕНИЯ
   ```

3. Отправьте сообщение боту в Telegram
4. Проверьте логи - должны появиться сообщения о получении и обработке

### 7. Мониторинг

- **Logs**: Все логи бота доступны в реальном времени
- **Metrics**: Railway показывает использование CPU/RAM
- **Deployments**: История всех развертываний

## Важные моменты

### ✅ Бот работает автономно
- Использует **long polling** (`getUpdates`) - идеально для Railway
- Автоматический перезапуск при ошибках
- Работает 24/7 без вашего участия

### 🔒 Безопасность
- Все API ключи хранятся в переменных окружения Railway
- Никакие секреты не попадают в код
- Railway шифрует переменные окружения

### 💰 Стоимость
- Railway предоставляет бесплатный тариф ($5 кредитов/месяц)
- Для простого бота этого достаточно
- При превышении лимита можно перейти на платный план

### 🐛 Решение проблем

**Бот не отвечает:**
1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `BOT_TOKEN` и `CHAT_ID` правильные

**Ошибки транскрипции:**
- Убедитесь, что установлен хотя бы один API ключ для транскрипции
- Или бот будет использовать бесплатный Hugging Face (может быть медленнее)

**Бот перезапускается:**
- Проверьте логи на ошибки
- Убедитесь, что все API ключи валидные
- Railway автоматически перезапустит бота (до 10 раз)

## Обновление бота

При каждом push в GitHub Railway автоматически:
1. Пересоберет проект
2. Установит новые зависимости
3. Перезапустит бота с новым кодом

Просто делайте `git push` и Railway все сделает автоматически!

