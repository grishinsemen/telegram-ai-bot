# ☁️ Развертывание бота на Cloudflare Workers

## ⚠️ Важные ограничения

Cloudflare Workers имеют ограничения:
- ⏱️ **Время выполнения**: до 30 секунд (бесплатный план) или 15 минут (платный)
- 📦 **Размер**: до 10MB (бесплатный) или 50MB (платный)
- 🔄 **Режим работы**: Только **webhook** (Telegram отправляет обновления), не long polling

**Важно**: Бот переделан на webhook режим для работы с Cloudflare Workers.

---

## ШАГ 1: Регистрация на Cloudflare

1. Откройте https://dash.cloudflare.com
2. Войдите или создайте аккаунт
3. Перейдите в раздел **"Workers & Pages"**

---

## ШАГ 2: Создание Worker

1. Нажмите **"Create application"**
2. Выберите **"Create Worker"**
3. Введите имя: `telegram-ai-bot`
4. Нажмите **"Deploy"**

---

## ШАГ 3: Загрузка кода

1. В редакторе Worker удалите весь код по умолчанию
2. Скопируйте содержимое файла `cloudflare_worker.js` из репозитория
3. Вставьте код в редактор
4. Нажмите **"Save and deploy"**

---

## ШАГ 4: Настройка переменных окружения

1. В панели Worker перейдите в **"Settings"** → **"Variables"**
2. В разделе **"Environment Variables"** добавьте:

### Обязательные:

| Variable Name | Value |
|---------------|-------|
| `BOT_TOKEN` | `8589117030:AAE-9u3JcHKVRZWpdMoiLF8x7dLBamLoAPo` |
| `CHAT_ID` | `-1001282173394` |
| `ZENMUX_API_KEY` | `sk-ai-v1-c458920c23c22f3241339c599348d56a3f79d69bdc4ecd053230be5e17017e06` |

### Рекомендуемые:

| Variable Name | Value |
|---------------|-------|
| `ZENMUX_MODEL` | `google/gemini-3-pro-preview-free` |
| `ZENMUX_BASE_URL` | `https://zenmux.ai/api/v1` |
| `OPENROUTER_API_KEY` | `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` |
| `OPENROUTER_MODEL` | `openai/gpt-4o-mini` |
| `PERSONALITY` | `putin` |

3. Нажмите **"Save"** после добавления каждой переменной

---

## ШАГ 5: Настройка Webhook в Telegram

1. Получите URL вашего Worker:
   - В панели Worker найдите раздел **"Triggers"**
   - Скопируйте URL (например: `https://telegram-ai-bot.your-subdomain.workers.dev`)

2. Установите webhook через Telegram API:
   ```bash
   curl -X POST "https://api.telegram.org/bot8589117030:AAE-9u3JcHKVRZWpdMoiLF8x7dLBamLoAPo/setWebhook?url=https://telegram-ai-bot.your-subdomain.workers.dev"
   ```

   Или используйте браузер:
   ```
   https://api.telegram.org/bot8589117030:AAE-9u3JcHKVRZWpdMoiLF8x7dLBamLoAPo/setWebhook?url=https://telegram-ai-bot.your-subdomain.workers.dev
   ```

3. Проверьте, что webhook установлен:
   ```
   https://api.telegram.org/bot8589117030:AAE-9u3JcHKVRZWpdMoiLF8x7dLBamLoAPo/getWebhookInfo
   ```

---

## ШАГ 6: Проверка работы

1. Откройте вкладку **"Logs"** в Cloudflare Dashboard
2. Отправьте сообщение боту в Telegram
3. Проверьте логи - должны появиться записи об обработке

---

## ✅ Готово!

Бот теперь работает на Cloudflare Workers!

### Преимущества Cloudflare Workers:

- ✅ **Бесплатный тариф** с 100,000 запросов/день
- ✅ **Быстрая работа** (глобальная сеть Cloudflare)
- ✅ **Автоматическое масштабирование**
- ✅ **Не засыпает** (в отличие от некоторых платформ)

### Ограничения:

- ⚠️ **Нет поддержки голосовых сообщений** (ограничения Workers)
- ⚠️ **Только webhook режим** (не long polling)
- ⚠️ **Ограничение времени выполнения** (30 сек бесплатно)

---

## 🔄 Обновление кода

1. Откройте Worker в Cloudflare Dashboard
2. Внесите изменения в код
3. Нажмите **"Save and deploy"**

---

## 🐛 Решение проблем

### Бот не отвечает:

1. Проверьте логи в Cloudflare Dashboard
2. Убедитесь, что webhook установлен правильно
3. Проверьте переменные окружения
4. Проверьте, что `BOT_TOKEN` и `CHAT_ID` правильные

### Ошибки в логах:

- Проверьте формат переменных окружения
- Убедитесь, что все API ключи валидные
- Проверьте, что URL webhook правильный

### Webhook не работает:

1. Проверьте URL Worker (должен быть доступен)
2. Убедитесь, что Worker развернут
3. Проверьте настройки CORS (если нужно)

---

## 📝 Отключение webhook (возврат к long polling)

Если хотите вернуться к long polling на другой платформе:

```bash
curl -X POST "https://api.telegram.org/bot8589117030:AAE-9u3JcHKVRZWpdMoiLF8x7dLBamLoAPo/deleteWebhook"
```

---

## 💡 Полезные ссылки:

- Cloudflare Dashboard: https://dash.cloudflare.com
- Документация Workers: https://developers.cloudflare.com/workers/
- Ваш репозиторий: https://github.com/grishinsemen/telegram-ai-bot

