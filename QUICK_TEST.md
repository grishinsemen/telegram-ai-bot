# Быстрый тест MiniMax MCP

## ✅ Текущий статус

- ✅ Python установлен (3.11.4)
- ✅ uvx установлен (0.9.11)
- ✅ Конфигурация MCP создана
- ✅ API ключ настроен

## 🚀 Тестирование

### Шаг 1: Перезапустите Cursor

**ВАЖНО**: Полностью закройте Cursor (все окна) и запустите снова.

### Шаг 2: Проверьте подключение MCP

1. Откройте Cursor
2. Нажмите `Ctrl+Shift+P` (Command Palette)
3. Введите: `MCP` или `Model Context Protocol`
4. Найдите команду для просмотра MCP серверов
5. Убедитесь, что сервер `minimax` отображается как подключенный

### Шаг 3: Проверьте доступность инструментов

После перезапуска Cursor должен автоматически подключиться к MiniMax MCP.

Доступные инструменты:
- `text_to_audio` - преобразование текста в речь

### Шаг 4: Тестовый запрос

Попробуйте использовать MCP через AI ассистента в Cursor:

```
Преобразуй текст "Привет, это тест MiniMax Text2Speech" в аудио используя MCP
```

Или напрямую через MCP инструменты, если они доступны в интерфейсе.

## 🔍 Проверка логов

Если что-то не работает:

1. Откройте Developer Tools в Cursor (`Help` → `Toggle Developer Tools`)
2. Проверьте вкладку `Console` на наличие ошибок MCP
3. Проверьте вкладку `Network` на наличие запросов к MCP серверу

## 📝 Конфигурация

Текущая конфигурация находится в:
```
C:\Users\SEMEN\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

## ⚠️ Возможные проблемы

### MCP сервер не подключается

1. Проверьте, что `uvx` доступен в PATH
2. Убедитесь, что Python установлен
3. Проверьте логи Cursor на ошибки
4. Попробуйте использовать полный путь к `uvx` в конфигурации

### Ошибка при запуске minimax-mcp

- Проверьте интернет-соединение (нужно для загрузки пакета)
- Убедитесь, что API ключ правильный
- Проверьте, что у вас есть доступ к MiniMax API

## ✅ Готово к использованию!

После перезапуска Cursor всё должно работать. MCP сервер подключится автоматически и инструмент `text_to_audio` будет доступен.

















