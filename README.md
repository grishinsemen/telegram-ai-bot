# Telegram AI Bot

Умный Telegram бот с поддержкой голосовых сообщений, транскрипции и множественных LLM провайдеров.

## 🚀 Возможности

- **Множественные LLM провайдеры**: ZenMux.ai, OpenRouter, Groq с автоматическим fallback
- **Транскрипция голосовых сообщений**: AssemblyAI, Deepgram, Lemonfox.ai, Hugging Face
- **Обработка голосовых сообщений**: Автоматическая транскрипция и ответ
- **Гибкая конфигурация**: Настройка через JSON файл
- **Персонализация**: Настраиваемая личность бота

## 📋 Требования

- Python 3.8+
- FFmpeg (для конвертации аудио)
- API ключи от выбранных провайдеров

## ⚙️ Установка

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd myrep
```

2. Установите зависимости:
```bash
pip install requests
```

3. Настройте конфигурацию:
```bash
# Скопируйте пример конфига
copy telegram_config.json.example telegram_config.json

# Отредактируйте telegram_config.json и добавьте свои API ключи
```

4. Запустите бота:
```bash
# Windows
start_bot.bat

# Или напрямую
python telegram_bot.py
```

## 🔧 Конфигурация

Создайте файл `telegram_config.json` на основе `telegram_config.json.example`:

- **bot_token**: Токен бота от @BotFather
- **chat_id**: ID чата для отправки сообщений
- **zenmux_api_key**: API ключ от ZenMux.ai (основной провайдер)
- **openrouter_api_key**: API ключ от OpenRouter (fallback)
- **assemblyai_api_key**: API ключ от AssemblyAI для транскрипции (опционально)
- **deepgram_api_key**: API ключ от Deepgram для транскрипции (опционально)
- **lemonfox_api_key**: API ключ от Lemonfox.ai для транскрипции (опционально)
- **personality**: Личность бота (putin, default, friendly, professional, funny)

## 📝 Использование

Бот автоматически:
- Отвечает на текстовые сообщения
- Транскрибирует и отвечает на голосовые сообщения
- Использует fallback провайдеры при ошибках
- Сохраняет контекст беседы

## 🔐 Безопасность

⚠️ **ВАЖНО**: Файл `telegram_config.json` содержит секретные ключи и **НЕ должен** попадать в git репозиторий. Он автоматически исключен через `.gitignore`.

## 📚 Структура проекта

- `telegram_bot.py` - Основной файл бота
- `telegram_config.json` - Конфигурация (не в git)
- `telegram_config.json.example` - Пример конфигурации
- `start_bot.bat` - Скрипт запуска для Windows
- `.gitignore` - Исключения для git

## 🛠️ Разработка

Для тестирования:
```bash
python test_bot_response.py
```

## 📄 Лицензия

MIT

